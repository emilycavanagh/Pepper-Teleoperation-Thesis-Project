# cli

## Getting Started

```sh
$ make install
$ make activate
```

## Update protobuf

- Run `make proto` from the `server` directory
- Replace the `cli/src/proto` directory from `server/src/proto`

## Try it out!

```sh
$ make start
```
